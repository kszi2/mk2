- if true_button?
  %button.w-fit.h-fit{id: @id, name: @name, **button_type, class: "group/button #{rounding_styles} #{size_styles} #{color_styles}", data: @data, disabled: !@enabled}
    - if prefix?
      = prefix
    = real_content
- else
  %a.block.w-fit.h-fit{id: @id, name: @name, class: "group/button #{rounding_styles} #{size_styles} #{color_styles}", href: @href, data: @data}
    - if prefix?
      = prefix
    = real_content
