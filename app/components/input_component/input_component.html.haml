- if label?
  %label(for=@id class=error_text_style)= label
%input.bg-wa-surface-default.rounded.border{id: @id,
  type: effective_type,
  name: @name,
  value: @value,
  class: "#{error_border_style} #{size_styles}",
  disabled: !@enabled
}
