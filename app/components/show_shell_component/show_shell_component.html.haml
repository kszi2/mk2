= shell_body do
  %article.flex.flex-col.gap-2
    - properties.each do |property|
      = property

  = resource_controls

  %aside.mt-4
    - customs.each do |custom|
      = custom

- if extras?
  %aside.mt-4
    = extras
