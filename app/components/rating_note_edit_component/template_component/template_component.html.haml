.w-full.border.border-surface-border.rounded.bg-surface-default.select-none{data: {
  controller: "template",
  action: "click->template#setContentToTemplate click->sidebar#close",
  template: {
    templates_outlet: "#" + @holder_id,
    cost: { value: @template.cost }
  }
}}
  .text-lg.my-2.border-b.mx-3.border-surface-border= @template.name
  .mb-3.mx-3{data: {
    template: {
      target: "data"
    }
  }}= @template.data
