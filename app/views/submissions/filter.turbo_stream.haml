= turbo_stream.replace "submission-listing" do
  #submissions-listing
    = render DataTableComponent.new(objects: @submissions,
                                    parents: [@course, @group],
                                    type: Submission,
                                    col_config: [ { field: :student, render_with: :name },
                                      { field: :coursework, render_with: :name },
                                    ])
