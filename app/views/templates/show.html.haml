= mk_show_for @template do |f|
  - f.with_property :name
  - f.with_property :course, name: "Connected course"
  - f.with_property :cost

  - f.with_custom name: "" do
    = render Inputs::TextareaComponent.new(name: "Data", value: @template.data, enabled: true, id: object_id, class: "w-full")
