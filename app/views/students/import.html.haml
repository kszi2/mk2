= turbo_frame_tag "student-imports", class: "w-full" do
  = render 'import_formats'

  = form_tag bulk_create_students_path, method: :post, multipart: true, class: "mt-4" do
    .flex.flex-col.gap-2
      = file_field_tag :file, accept: "text/csv,text/html"
      = submit_button_tag "Import"
