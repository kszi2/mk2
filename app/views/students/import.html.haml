= turbo_frame_tag "student-imports" do
  = form_tag bulk_create_students_path, method: :post, multipart: true do
    .flex.flex-col.gap-2
      = file_field_tag :file
      = submit_button_tag "Import"
