= render BreadcrumbsComponent.new(path: [ [@course_type.course.name, course_path(@course_type.course)],
                                          [@course_type.name, course_course_type_path(@course, @course_type)], ])

= turbo_frame_tag(:course_type_body) do
  .mt-4
    = render ResourceControlsComponent.new(edit: edit_course_course_type_path(@course, @course_type),
                                           back: course_path(@course, from: :course_type))
