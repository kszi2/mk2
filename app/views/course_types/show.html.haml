= turbo_page_requires_reload

%p
  %b Course:
  %span= @course_type.course.name
%p
  %b Name:
  %span= @course_type.name

.flex.gap-2.mt-4
  %wa-button{size: :small, href: edit_course_course_type_path(@course, @course_type)} Edit
  %wa-button{size: :small, href: course_path(@course, page: :course_types)} Back
