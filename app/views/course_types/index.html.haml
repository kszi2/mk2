%h1.text-2xl Listing course_types

= turbo_frame_tag "course-types-frame" do
  = render DataTableComponent.new(objects: @course_types,
                                  parents: [@course],
                                  type: CourseType,
                                  col_config: [ { field: :name } ])
