= turbo_frame_tag "marked-points-frame-inner" do
  = render RatingNoteEditComponent.new(note: @marking_note,
                                       url: course_group_submission_marked_point_marking_notes_path(@course, @group, @submission, @marked_point),
                                       method: :post)
